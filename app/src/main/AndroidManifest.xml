<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.varunbarad.attendancetracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <activity
            android:name=".splash.SplashActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".mainattendance.MainAttendanceActivity">
        </activity>
        <activity android:name=".subject.listsubjects.SubjectsListActivity">
        </activity>
        <activity android:name=".subject.addsubject.AddSubjectActivity">
        </activity>
        <activity android:name=".subject.subjectdetails.SubjectDetailsActivity">
        </activity>
        <activity android:name=".subject.editsubject.EditSubjectActivity">
        </activity>
        <activity android:name=".settings.SettingsActivity">
        </activity>

        <receiver android:name=".widget.AttendanceWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE"/>
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/attendance_widget_info"/>
        </receiver>

        <activity
            android:name=".widget.subjectselect.SubjectSelectActivity"
            android:label="@string/title_subjectsList">
        </activity>

        <provider
            android:name=".data.sqlite.AttendanceProvider"
            android:authorities="com.varunbarad.attendancetracker"
            android:exported="false"/>

        <activity android:name=".about.DeveloperActivity">
        </activity>
    </application>

</manifest>